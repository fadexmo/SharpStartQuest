# NetSharpWeb

Application web ASP.NET Core Razor Pages avec .NET 10.

## Lancer l'application

```bash
dotnet run
```

Ou avec un port fixe :

```bash
dotnet run --no-launch-profile --urls http://localhost:5050
```

## Compiler

```bash
dotnet build NetSharp.slnx
```
